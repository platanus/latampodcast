<% @podcasts.each do |podcast| %>
  <div class="podcast-container">
    <%= image_tag("#{podcast.image_slug}") %>
    <div class="podcast-text-container">
      <h2><%= podcast.title %></h2>
      <p><%= podcast.description %></p>
      <div class="podcast-info-footer">
        <p><%= 'Posted Dec 19, 2018' %></p>
        <span></span>
        <p><%= '#Features' %></p>
      </div>
    </div>
  </div>
<% end %>